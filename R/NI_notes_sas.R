# encoding for filer (UTF-8)
# konsistens i navngiving av objekter. ikke punktum,
# ikke .fct
# ikke norske navn mere generelle navn på argumenter: N.Omr N.kom FK_OmraadeID Scaled.ind.per.mun  også stor S kvantiler
# endre funksjonsnavn til verb, NIcalculation -> NIcalculate
# :: istedet for importFrom
# If you are using just a few functions from another package, the recommended
# option is to note the package name in the Imports: field of the DESCRIPTION
# file and call the function(s) explicitly using ::, e.g., pkg::fun().
# Alternatively, though no longer recommended due to its poorer readability, use
# @importFrom, e.g.,
# @importFrom pgk fun, and call the function(s) without ::.
